# proofs/INDEX.md — claim → file → status ledger

Every claim from the hexagon-lattice plan that has a Lean file. Status: PROVED
(`lake build` green) / PORTED (statement moved in, proof pending) /
STATED-UNPROVED (contract only) / RETIRED.

**Build state (2026-08-29): `lake build` GREEN** (8749 jobs, toolchain v4.33.1, zero
`sorry`). The stale elan override to v4.34.0-rc2 was cleared (SETUP.md troubleshooting).
ALL targets proved: T0–T6 + Gauge + Residual + Bijection + Registers + ConventionBridge +
FractalRam + TernaryCell (energy ≤1 line, null free, avg 2/3) + the measure/energy batch
(Haar: counting measure is Z₆-invariant; ChiSquareGauge: δ gauge-invariant, χ² not;
ValuationEnergy: energy is a lattice valuation, min+max=sum; ZipfEnergy: expected energy
= 1−P(null)) + the RAM-geometry batch (Pod: norm≤1 = 7-of-9 axial states + 2 spare
norm-3 carry states; HexIsotropy: free Z₆ action + pod rotation-invariance; HexDisk:
centered hexagonal number 3r²+3r+1; OffsetGrid: hex ≃ offset square grid; CausalLattice:
the hex causal lattice — flow/curl, diamond balance, pod = causal diamond; AddressTranslation:
the hex↔u32 address translation — angle refines parity, CRT assembly, ring growth; FieldCalculus:
the TGRAD/TRECON field-calculus pair — div⊕curl, canonical ∇⁻¹ round-trip — see table).
Fixed: `WeightHex.lean`'s `def omega` collided with `Gauge.lean`'s `def omega` under the
umbrella import — renamed WeightHex's to `omegaUnit`. Rust mirror in `../rust-mirror/`
(`cargo test` 29 green). Remaining open: T6 geometric derivation + Thue optimality
(cited); the hex↔u32-XOR replacement claim (still SPECULATION).

| # | Claim | Calibration | File | Status (2026-08-28) | Origin |
|---|---|---|---|---|---|
| T0 | ℤ[x]/(x²−x+1) is a commutative ring (the Eisenstein integers) | DIRECT | `Hexagon/Conventions.lean` | **PROVED** (`mul_comm`, `ext <;> dsimp [mul] <;> ring`) | hexigon L10005–10105; TODO #16 (ox alpha L3109) |
| T1 | N(a+bω) = a²+ab+b² is multiplicative | DIRECT | `Hexagon/Conventions.lean` | **PROVED** (`norm_mul`, `dsimp [norm, mul]; ring_nf`) | hexigon L11544 (Eisenstein norm); plan §3 |
| T2a | There are exactly 7 hex cells | DIRECT | `Hexagon/SevenHex.lean` | **PROVED** (`hexCells_card`, `decide`) | hexigon L10005–10105 |
| T2b | Balanced-ternary triples (q,r,s), q+r+s=0, ↔ the 7 hex cells | DIRECT (enumeration-proved by hand) | `Hexagon/SevenHex.lean` | **PROVED** (`balanced_iff_mem`, `rcases` + `fin_cases` + `decide`) | hexigon L10005–10105 (the one-page theorem L10105) |
| T3a | The six units ±1, ±ω, ±ω² (exactly six) | DIRECT | `Hexagon/Rotation.lean` | **PROVED** (`units_card`, `decide`) | hexigon L10179–10197, 11248, 11397–11399 |
| T3b | The units form Z₆; rotation = mod-6 angle add | DIRECT | `Hexagon/Rotation.lean` | **PROVED** (`units_closed_under_mul`, `fin_cases` + `decide`) | hexigon L10179–10197, 11248, 11397–11399 |
| T4 | Cube-coordinate max-norm distance = honeycomb graph distance | DIRECT | `Hexagon/GraphDistance.lean` | **PROVED** (`honeycomb_dist_eq_hexDist` — both directions: greedy upper bound + walk-induction lower bound; metric lemmas in `Rotation.lean`) | plan §3; SYNTHESIS Q1 (hex addressing prerequisite) |
| T5 | ℤ[ω] is a Euclidean domain / UFD | DIRECT (classical) | `Hexagon/EuclideanDomain.lean` | **PROVED** (`instCommRing` + `EuclideanDomain` instance via the pure-ℝ route: conjugate `star_mul_self` + `norm_mod_lt` from `exists_near_int_pair`; UFD/PID free) | plan §5; mirrors mathlib `GaussianInt` |
| T6 | Hexagonal lattice packing density = τ/(4√3) (= π/(2√3)) | DIRECT (number) / optimality: cited theorem | `Hexagon/Packing.lean` | PARTIAL — `tau` + `hexPackingDensity` + τ identity `hexPackingDensity_eq_tau_div` PROVED; geometric derivation + Thue optimality deferred (cited) | plan §5; Thue/Fejes Tóth; Ian (τ not π) |
| G1 | Isotropy: N(u)=1, N(u·x)=N(x), N = det of regular rep, units = ω⁰…ω⁵ | DIRECT | `Hexagon/Gauge.lean` | **PROVED** (`norm_of_unit`, `norm_mul_unit`/`norm_unit_mul`, `norm_eq_det`, `units_eq_omega_powers`) | Ian "gauge as area scalar"; TODO #16 |
| R1 | Rebuild residual: E marginal, Σ(O−E)=0, wedge skew, χ²≥0 | DIRECT | `Hexagon/Residual.lean` | **PROVED** (`sum_E_row`, `sum_residual_eq_zero`, `wedge_antisymm`, `ringSq_nonneg`) | LATTICE_MATH.md; ox alpha "column balancing" |
| R2 | Register ladder: δ fold, surprise=δ²·E, sign-collapse, wedge skew split | DIRECT | `Hexagon/Registers.lean` | **PROVED** (`δ_eq_residual_div`, `surprise_eq_delta_sq_mul_E`, `surprise_sign_collapse`, `sym_plus_skew`) | gauge.rs register stack; LATTICE_MATH.md |
| B1 | hex ↔ ℕ (u32) address bijection (Szudzik pairing) | DIRECT (bijection) / SPECULATION (replaces u32 XOR kernel) | `Hexagon/Bijection.lean` | **PROVED** (`hexPairEquiv`, `toNat_bijective`, `eisensteinEquiv`; exact u32 box `toNat_lt_two_pow_32`) | plan §2.2; SYNTHESIS Q1 |
| C1 | 60° ≅ 120° Eisenstein — the same ring | DIRECT (ring isomorphism) | `Hexagon/ConventionBridge.lean` | **PROVED** (`phi_add`, `phi_mul`, `phi_phi`, `norm_preserved` via φ(a,b)=(a,−b)) | gauge_int.rs 120° vs our 60°; Ian "all the same" |
| FR1 | Fractal hex RAM: 7ⁿ address space, 7↔1 parent-child | DIRECT (combinatorics) | `Hexagon/FractalRam.lean` | **PROVED** (`fractalAddress_card`, `parent_fiber_card`, `level_succ_card`, `levelOneEquiv`) | hexigon L11971–12123; TERNARY_PROCESSOR.md |
| TC1 | Ternary cell: one-hot-per-direction, energy ≤1 line, null free, avg 2/3 vs binary 1 | DIRECT (combinatorics) | `Hexagon/TernaryCell.lean` | **PROVED** (`energy_pos/zero/neg`, `energy_le_one`, `null_is_free`, `average_energy`, `ternary_saves_third`, `encode_never_both`, `encode_injective`, `encode_not_surjective`) | Ian "one-hot-per-direction"; TERNARY_PROCESSOR §1.2 |
| A1 | Counting measure is Z₆-invariant (Haar); each unit has a unit inverse | DIRECT (finite group) | `Hexagon/Haar.lean` | **PROVED** (`unit_inv`, `mul_unit_bijective`, `measure_invariant_card`, `units_counting_normalized`) | measure-theory synthesis; 2506.15534 |
| A2 | The fold δ is gauge-invariant; χ² is not (it scales) | DIRECT (algebra) | `Hexagon/ChiSquareGauge.lean` | **PROVED** (`fold_gauge_invariant`, `surprise_scales`, `fold_eq_surprise_div`) | gauge.rs "store δ not r" |
| A3 | Energy is a lattice valuation; min+max=sum (the tadd1 identity) | DIRECT (lattice theory) | `Hexagon/ValuationEnergy.lean` | **PROVED** (`min_add_max`, `tritVal_min_add_max`, `energy_min_max`, `energy_monotone` in the cost order) | 1903.06044 Lemma 212 |
| B1z | Zipf-weighted energy: expected energy = 1−P(null) < 2/3 when null dominates | DIRECT (expectation) | `Hexagon/ZipfEnergy.lean` | **PROVED** (`expected_energy_eq_one_minus_pzero`, `expected_lt_uniform`, `expected_concrete`) | Ian "small numbers outweigh big" |
| B2 | Radix economy: ternary beats binary per bit; quaternary ties | DIRECT (`Real.log`) | `Hexagon/RadixEconomy.lean` | **PROVED** (`ternary_beats_binary`, `quaternary_ties_binary`, `bits_per_trit_gt_one`) | 1807.06419; ternary synthesis |
| B4 | The four signatures (i²=−1,+1,0,ω): units 4/4/2/6, zero-divisor/nilpotent split | DIRECT (finite rings) | `Hexagon/Signature.lean` | **PROVED** (`gaussianUnits_card`, `split_zero_divisor`, `dual_nilpotent`, `dualUnits_card`, `signatures_distinguished`) | gauge_int.rs `Signature`; GAUGE_VARIANTS §1 |
| — | Hex lattice replaces the u32 XOR kernel | SPECULATION (structural half now PROVED) | `Hexagon/AddressTranslation.lean` (angle⊇parity, CRT assembly, bijection, ring) | was BLOCKED on T4 + address-translation theorem — **both now PROVED**; the *performance-equivalence* half stays SPECULATION | SYNTHESIS Q1; plan §2.2 |
| — | "Hexagonal causal lattice" light cone | RETIRED (contradicts Rung-1) | — | RETIRED | SYNTHESIS Q3; plan §2.3 |
| — | Quantum speedup via superposition | SPECULATION (author: "just a suspicion") | — | EXCLUDED from scope | plan §2.4 |
| — | One-wire ternary (push/pull/null) → 2-diode binary | hardware layer (hex-mmu), not Lean | — | RECORDED (plan §3, §7) | Ian 2026 reply |
| — | Pod: norm ≤ 1 = 7 of the 9 axial {-1,0,1}² states; 2 spare norm-3 carry states | DIRECT | `Hexagon/Pod.lean` | **PROVED** (`pod_card`, `pod_norm_le_one`, `axialNine_eq_pod_union_spare`, `spare_norm`, `norm_le_one_iff_mem`) | Ian "7 trits pod" + "6 for redundancy" |
| — | Isotropic lookup: every cell has exactly 6 distinct unit neighbors (free Z₆ action); pod is rotation-invariant | DIRECT | `Hexagon/HexIsotropy.lean` | **PROVED** (`translate_injective`, `neighbors_card`, `no_fixed_point`, `units_rotate_invariant`) | Ian "isotropic lookup, 6 directions" |
| — | Hex disk area: centered hexagonal number 3r²+3r+1 (radius r → 1,7,19,37,…) | DIRECT | `Hexagon/HexDisk.lean` | **PROVED** (`hexDiskCard_eq`, `hexDiskCard_succ`, `hexDiskCard_quadratic`) | Ian "grab by area, not scalar and offset" |
| — | Hex lattice ≃ offset square grid (checkerboard brick wall, (a,b)↦(2a+b,b)) | DIRECT | `Hexagon/OffsetGrid.lean` | **PROVED** (`offset_injective`, `offset_image_iff`, `offset_offsetInv`, `offsetInv_offset`) | Ian "columns of squares 0.5 off" |
| — | Z₆ ≅ Z₂ × Z₃ (CRT): every unit = a sign (±1) × a 3-cycle (1,ω,ω²), (s,c)↦s·c bijects sign×cycle → units; angle index n ∈ Fin 6 ≃ Fin 2 × Fin 3 via (n mod 2, n mod 3) | DIRECT | `Hexagon/CrtHex.lean` | **PROVED** (`signCycleMul_surjective`, `signCycleMul_injective`, `signCycle_card`, `mod6_iff_mod2_mod3`, `modPair_bijective` — `fin_cases`/`decide`) | Ian "six units are ±1 times 1,ω,ω²"; CRT (2,3 coprime) |
| — | Polar ↔ balanced ↔ 2-bit translation: trit {−1,0,+1} ↦ 01/00/10, with 11 the never/don't-care | DIRECT | `Hexagon/PolarEncoding.lean` | **PROVED** (`polarEncode_injective`, `polarEncode_never_eleven`, `polarEncode_not_surjective`, `polarDecode_encode` — `fin_cases`/`decide`) | Ian "one-hot-per-direction"; `TernaryCell.lean` |
| — | Ternary-memory density bounds: 3⁴≤2⁷, 3⁵≤2⁸, 3ⁿ≤2²ⁿ (n trits fit 2n bits), 2ⁿ<3ⁿ (ternary denser per symbol) | DIRECT | `Hexagon/TritPacking.lean` | **PROVED** (`four_trits_fit_seven_bits`, `five_trits_fit_eight_bits`, `three_pow_le_two_pow_two_mul`, `three_pow_gt_two_pow` — `norm_num`/induction/`omega`) | ternary-memory survey; 1807.06419 |
| — | Radix economy is minimized at e: `b/ln b` has derivative `(ln b − 1)/(ln b)²`, minimum at `b=e`; hence `e < 3/ln 3` (the closest integer to e is 3) | DIRECT | `Hexagon/RadixMin.lean` | **PROVED** (`deriv_radix_economy`, `radix_economy_min_at_e`, `exp_one_lt_three_div_log_three` — `HasDerivAt`/`nlinarith`) | euler_constants survey; Law 3 |
| — | Compute threshold lower bound: thresholds-per-bit `(b−1)/ln b` is strictly increasing for `b>1`, so binary (b=2) minimizes it and ternary is `2·ln 2/ln 3 ≈ 1.26×` worse (the formal 2-threshold tax) | DIRECT | `Hexagon/ThresholdLowerBound.lean` | **PROVED** (`deriv_threshold_per_bit`, `threshold_per_bit_mono`, `binary_min_threshold_per_bit`, `ternary_worse_than_binary`, `ternary_binary_ratio` — `HasDerivAt`/`strictMonoOn_of_deriv_pos`) | ground-up batch; the "2-threshold tax" made formal |
| — | The ℂ-embedding: ω ↦ e^{iπ/3} is a ring homomorphism; ω²=ω−1 holds in ℂ; φ is injective (ℤ[ω] embeds as the 60° lattice) | DIRECT | `Hexagon/OmegaEmbedding.lean` | **PROVED** (`omega_sq_rel`, `phi_add`, `phi_mul`, `phi_omega`, `phi_injective` — Euler's formula + `ring`/`norm_num`) | euler_constants survey; τ/6 ↔ ω |
| — | Balanced-ternary gate semantics (namespace `PolarGate`, residue 0=0,1=+1,2=−1): neg/min/max/mod-3 sum/product + the F₃ field identities (involution, comm, absorption, distributivity) | DIRECT | `Hexagon/PolarGate.lean` | **PROVED** (`neg_neg`, `tmin_comm`, `tsum_comm`, `tsum_zero`, `tsum_neg`, `tmax_idem`, `tmin_absorb`, `neg_tsum`, `tprod_distrib`, `rot1_three`, `rot2_three`, `tprod_comm` — `fin_cases`/`decide`) | Ian "polar ternary gate semantics"; ground-up batch |
| — | Fewer symbols: `2^k ≤ 3^(k−1)` for `k≥3` (k−1 trits ≥ k bits of states); namespace grows geometrically `3^(n+1)/2^(n+1) = (3/2)·(3^n/2^n)` | DIRECT | `Hexagon/FewerTrits.lean` | **PROVED** (`two_pow_le_three_pow_pred`, `fewer_trits_than_bits`, `three_pow_div_two_pow_mono` — induction/`omega`/`field_simp`) | Ian "3ⁿ explodes"; namespace table |
| — | The Eisenstein conjugate `conj(a,b)=(a+b,−b)` (`TCONJ`): involution, norm-preserving, `conj(z·w)=conj z·conj w`, and `z·z̄ = N(z)` | DIRECT | `Hexagon/Conjugate.lean` | **PROVED** (`conj_involutive`, `conj_norm`, `conj_mul`, `mul_conj_eq_norm` — `rcases`/`change`/`ext`/`ring`) | GA instructions (docs/GA_INSTRUCTIONS.md) |
| — | Dot & wedge (`TDOT`/`TWEDGE`): the geometric product `z·w̄` splits into skew (wedge) + scalar coordinate; `wedge_antisymm`, `dot_self=N(z)`, `wedge_self=0`, `dot²+dot·wedge+wedge²=N(z)N(w)`; NOTE the symmetric dot `Re(z·w̄)=dot+wedge/2` is half-integral, so `dot_comm` is FALSE — replaced by `dot_swap : dot z w = dot w z + wedge w z` | DIRECT | `Hexagon/DotWedge.lean` | **PROVED** (`gp_decomp`, `wedge_antisymm`, `dot_self`, `wedge_self`, `dot_sq_add_wedge_sq`, `dot_swap` — `rcases`/`change`/`ring`) | GA instructions; the wedge = skew/curl (Hestenes-Sobczyk) |
| — | The symmetric dot (`TDOT` correlation): `symdot z w = N(z+w) − N(z) − N(w)` (the polarization = `2·Re(z·w̄)` = `2ac+ad+bc+2bd`); symmetric, `symdot_self = 2N(z)`, `symdot = 2·dot + wedge`, nonnegative | DIRECT | `Hexagon/SymDot.lean` | **PROVED** (`symdot_comm`, `symdot_self`, `symdot_eq_two_dot_add_wedge`, `symdot_nonneg` — `rcases`/`change`/`ring_nf`/`nlinarith`) | GA instructions; fixes the half-integral `Re(z·w̄)` |
| — | Ternary CRT instantiation (the XOR→hex bridge): `val ds % 3 = t₀ % 3` (least trit), `val ds % 2 = digitSum % 2` (parity), `val ds % 6 = (3·(digitSum%2) + 4·(t₀%3)) % 6` (CRT assembly `3a+4b`); trit-shift ÷3 truncation `val ds = t₀ + 3·val(shift3 ds)` | DIRECT | `Hexagon/TernaryCrt.lean` | **PROVED** (`val_mod_three`, `val_mod_two`, `val_crt`, `val_add_shift3`, `div3_truncation` — `Int.ModEq`/`Finset.sum`/`ring`) | xor_barrel.md's #1 open target; the `n mod 6 = (S mod 2, t₀)` identity |
| — | The **hex causal lattice** (temporal precedence / circulation on ℤ[ω], replacing the "diamond lattice" of lasttheory 076): the 6 Z₆ unit directions are the directed causal edges; `flow z = Σ_u wedge(z, z+u)` (divergence), `curl z = F1+F2−F4−F5` (bivector circulation); the wedge is skew (`causal_skew`); the center's outflow is exactly balanced by the ring's backflow (`diamond_balance` = discrete Σ(O−E)=0 at the pod); the pod (center + 6 ring) is the causal diamond (`pod_is_causal_diamond`), Z₆-invariant | DIRECT (order theory + skew-symmetric algebra; Rung-1 — no light-cone/causal-arrow over-claim) | `Hexagon/CausalLattice.lean` | **PROVED** (`causal_isotropy`, `causal_skew`, `diamond_balance`, `pod_is_causal_diamond`, `causal_diamond_norm_le_one` — `Finset.sum_congr`/`change`/`ext`/`dsimp`/`omega`/`ring`) | Ian "replace the diamond lattice with the hex causal lattice" |
| — | The **address translation** (the hex↔u32 XOR-kernel bridge): the hex address's Z₆ angle (mod 6) refines the binary XOR phase (parity mod 2) — `n % 2 = (n % 6) % 2`; the angle is assembled from phase × 3-cycle by CRT — `n % 6 = (3·(n%2)+4·(n%3)) % 6`; `eisensteinToNat` is a bijection (same address space); the hex disk grows `3r²+3r+1` (the hex analog of the `2^k` rings) | DIRECT (modular arithmetic / CRT / combinatorics) | `Hexagon/AddressTranslation.lean` | **PROVED** (`angle_refines_parity`, `hex_angle_assembly`, `hex_address_bijective`, `hex_ring_growth`, `address_translation` — `Int.modEq`/`dvd_trans`/`norm_num`) | the missing bridge that unblocks "hex replaces the XOR kernel" (structural half) |
| — | The **field calculus** (TGRAD/TRECON/TRELAX, `rtl/grad_recon.v` + `rtl/trelax.v`): `div6 = F0−F2−F3+F5` (Re/source grade), `curl6 = F1+F2−F4−F5` (Im/skew grade); the canonical gauge-fixed ∇⁻¹ section `trecon(d,c)` places the source on ω⁰,ω¹; `lap = Σ_nb − 6·u` (the 6-point hex Laplacian, ∇²); `trelax = u/3 + Σ_nb/9` (the heat step). **`trecon_roundtrip`** (TRECON∘TGRAD = identity in canonical gauge), **`tgrad_trecon_tgrad`** (the gauge-invariant round trip), **`div_curl_shift_invariant`** (the additive gauge), **`lap_constant`** (Lap of a constant = 0 — the steady state), **`lap_add`** (the Laplacian is linear) | DIRECT (finite arithmetic over the 6 Z₆ ring cells) | `Hexagon/FieldCalculus.lean` | **PROVED** (`trecon_roundtrip`, `tgrad_trecon_tgrad`, `div_curl_shift_invariant`, `lap_constant`, `lap_add` — `simp`/`ring_nf`/`Finset.sum_add_distrib`) | closes the xlattice_encoding.md §4 "TRECON canonical section not yet a theorem" gap + the ∇²/TRELAX formalization |

| — | The **junction-polarity trit** (the P/N channel model): `Channel` (push/pull) + `Trit = Option Channel` — the one-hot sum `{push,pull,none}` where `11=NEVER` is *unrepresentable by construction*; single-activation (`energy_le_one`, `energy_eq_zero_iff_null`); bijection `balancedEquiv : Trit ≃ Hexagon.Trit`; negation is FREE (`neg_energy` — a pure wire relabel, `neg_encodes_swap`, `neg_neg`); null is the additive identity (`sum_null`/`sum_null_right`, `sum_push_pull` — opposites annihilate, `sum_comm`/`sum_assoc` — the channel sum is Z/3) | DIRECT (the one-hot push/pull/null encoding, `TernaryCell.lean`/`PolarEncoding.lean`) | `Hexagon/JunctionPolarity.lean` | **PROVED** (`energy_le_one`, `energy_eq_zero_iff_null`, `balancedEquiv`, `ofBalanced_toBalanced`, `toBalanced_ofBalanced`, `neg_energy`, `neg_encodes_swap`, `neg_neg`, `sum_null`, `sum_push_pull`, `sum_comm`, `sum_assoc` — `cases`/`decide`/`rfl`) | Ian "P/N junction — only one channel activates; negation = swap the field" |
| — | The **junction energy model**: `junctionCost e t` = 1 (±1, one channel) / `e` (null); `wordEnergy e w = 12 − nullCount·(1−e)`; the "single-channel activation makes ternary cheap" claim survives only as a CONDITIONAL theorem — `ternary_wins_iff` (break-even null fraction `p* = (1−b·c)/(1−e) ≈ 0.338 > 1/3`), `breakEven_above_uniform`, `at_uniform_not_cheaper` (strictly worse at uniform), `win_at_null_half` (strict win at p=1/2); polarity-invariance (`junctionCost_negate`, `junctionCost_relabel`); the 2-threshold sensing tax is additive + null-fraction-invariant (`transport_saving_does_not_erase_compute`) | DIRECT (measured 0.05/1.20 pJ + `ThresholdLowerBound.lean`) | `Hexagon/JunctionEnergy.lean` | **PROVED** (`structural_win`, `ternary_wins_iff`, `breakEven_above_uniform`, `at_uniform_not_cheaper`, `uniform_tie_gap`, `win_at_null_half`, `junctionCost_relabel`, `transport_saving_does_not_erase_compute` — `norm_num`/`nlinarith`/`ring`) | Ian "weight each op by energy; single-channel activation should save cost"; the ~33.8% null-fraction crossover |
| — | The **junction memory bound**: `expectedActive N p0 = N·(1−p0)` (expected energized channels under iid null-fraction `p0`, proved by induction over words); `expectedActive_lt_N` (any `p0>0` ⇒ < N channels while carrying `N·log₂3` bits); uniform = `2N/3`; the **2.7–6.3× transport number** (`champion_vs_natural = 512/81`, `champion_vs_lowswing = 8/3`, `transport_range`); `two_trit_break_even` (`p0 > 216/575 ≈ 0.376`); the `(3/2)ⁿ` rail ratio (`three_pow_div_two_pow`, `ternary_more_states_per_rail`) | DIRECT (measured 0.081/0.512/0.216 pJ/bit + `TritPacking.lean`) | `Hexagon/JunctionMemory.lean` | **PROVED** (`expectedActive`, `sum_prod_sum_linear`, `expectedActive_lt_N`, `uniform_expectedActive`, `champion_vs_natural`, `champion_vs_lowswing`, `transport_range`, `two_trit_break_even`, `three_pow_div_two_pow`, `three_pow_gt_sq` (3ⁿ > n²), `namespace_outruns_linear_cost` (∀C, ∃n, C·n < 3ⁿ — exponential info outruns linear overhead), `three_pow_gt_two_pow_succ` (2ⁿ⁺¹ < 3ⁿ for n ≥ 2 — the address-space crossover at the base of the lower power), `three_pow_lt_two_pow_succ_one` (3¹ < 2², so n=2 is exact) — induction/`norm_num`/`nlinarith`) | Ian "energise half the RAM — 2 bits but only activate one channel"; "info explodes past activation cost" |

| — | The **conversion-free polar transport** (Ian: "polar ternary has no 4th state; wire the trit gates straight to the binary bus"): `polarEncode` is injective with image exactly `{00,01,10}` (`polarEncode_image`, `11` excluded), so a ternary transport word IS a binary word (`encodeWord_injective`, `encodeWord_never_eleven` — 2 bits stay 2 bits, no decode/table at the transport layer); transport energy = the non-null count (`wordEnergyJ_eq_nonNullCount`, null `00`=0 wires); and the win is CONVERSION-FREE — a 12-trit word with ≥3 nulls (`conversion_free_transport_win`) or ≥5 nulls (`five_nulls_word_win`, the `ternary_wins_iff` break-even) beats the binary word carrying the same information, with zero translation overhead (the one-hot→signed decode is confined to the ALU boundary, not the wire) | DIRECT (`PolarEncoding.lean` + `JunctionEnergy.lean`/`JunctionPolarity.lean`) | `Hexagon/PolarTransport.lean` | **PROVED** (`polarEncode_image`, `encodeWord_injective`, `encodeWord_never_eleven`, `wordEnergyJ_eq_nonNullCount`, `wordEnergy_zero_eq`, `conversion_free_transport_win`, `conversion_free_transport_win_measured`, `five_nulls_word_win`, `two_nulls_beat_24bit_binary_word`, `beats_binary_same_wires` — `fin_cases`/`Finset.sum`/`nlinarith`) | Ian "polar ternary — no 4th state, wire straight to the bus, free decode" |

## Convention note (read before writing proofs)

ω = e^(iπ/3); ω² = ω − 1; N(a+bω) = a² + ab + b²; units Z₆; τ = 2π (the full-turn
constant — Ian's convention: the base unit of rotation is a circle, not a semicircle).
mathlib's `EisensteinInt` (if present) likely uses ω = e^(2πi/3), norm a² − ab + b²
— prefer reusing it and proving the convention bridge (T-ISO) over re-defining.
