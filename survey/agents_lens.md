# AGENTS.md Lens Pass — calibrated against the hexigon conversation

Lens: `survey/hexigon_graph.md` (map pass of `hexigon_conversation.md`, 15,635 lines).
Target: `/home/ian/opencode/parser/english/AGENTS.md` (3,094 lines; current canonical truth 2026-08-18/08-22, append-only with retired claims marked).
Method: stage-2 recheck per the Deep-Dive Method. Every row in §1 cites BOTH sides. Line ranges for AGENTS.md are AGENTS.md lines; conversation ranges cite `hexigon_graph.md` node/edge ranges (which themselves cite the source conversation).

**Headline finding (read first):** AGENTS.md already contains its own record of this exact conversation — "Chat-session survey #2 — 'gauge = the unit' (Gaussian/Eisenstein integer lattice, 2026-08-25)" at AGENTS.md L2821–2844 ("Surveyed a private chat session (31 turns — a solo derivation)"). The "Eisenstein triangles" and "simplex Aₙ" hunt targets are NOT external concepts: they are the project's own landing notes on the hexigon thread, with a concrete plan (`gauge-int` experiment + `Signature` axis + store spinor integer `(E,w)`). Calibration is therefore partly *internal*: the two documents are two views of one thread, and several "cross-mappings" are self-mappings.

---

## 1. Calibration table

Legend: **DIRECT** = same math/object (an identity); **ANALOGY** = parallel structure, different object; **OURS** = exists on one side only; **SPECULATION** = unproven/untested on either side. "Name collision" rows are flagged explicitly.

| # | AGENTS.md node (line range) | Conversation node (hexigon_graph range) | Verdict | Evidence, both sides |
|---|---|---|---|---|
| 1 | Gauge = register = ring shift, L1417–1425 ("One operation, three names: gauge transformation (physics) = register change (hardware) = ring shift `d >> n` (lattice)"; t-1..t2 P@K plateau, t3 singularity) | N37 "The gauge is the page table" (L8224–8230, "square gauge = flat page tables (x86/ARM); hex gauge = 7-level hexagonal tables; changing gauge changes the MMU walk") + N36 hex paging (L8147–8172) | **ANALOGY** | Both = "a change of indexing/reference scheme is a gauge", both hierarchical (AGENTS: 2ⁿ ring bands; conversation: 7ⁿ hex cells). But AGENTS's gauge is a *statistical normalization* — which δ-formula (raw/fold/z/surprise) + which scale, group R⁺/2^ℤ Abelian, invariant δ (L3035–3050, L3071–3075) — while the conversation's is a *physical address-space layout*. ZERO occurrences of "page table", "7^n", "DGGS", "isotropy", "ternary" in AGENTS.md: the conversation's page-table content has no AGENTS text at all. Also AGENTS's own gauge survey kills the rotor flavor: "The gauge group is R⁺ … ABELIAN … 'register shift = rotor' is wrong" (L3073–3075). |
| 2 | Gauge covariance / inner automorphism — "x→RxR⁻¹ is verbatim the gauge transformation, yet a scalar commutes with everything, so Ad(R) is the identity" (L2261–2264); diagonal-unitary gauge = Fourier shift (L2367–2369) | N26 gauge covariance O→UOU⁻¹ (L6959–7302, DIRECT in its own graph) | **DIRECT (algebra) / ACTIVE-vs-INERT** | Same conjugation operation. AGENTS has already *proved* its own rotor is grade-0, so Ad(R)=identity (inert); the conversation's operators genuinely act. The mapping is direct *as algebra*; the active/inert gap is the calibration caveat. This is the one hard DIRECT point between the two gauge vocabularies. |
| 3 | Bit-shift RG: "Shifting `d >> 1` … is Renormalization Group (RG) flow" (L555–558, L905–906); **PROVEN: `d>>1` IS the 2-adic hierarchical RG blocking step** (L2330–2334); M5 threshold sweep retired as flow (L2336–2339); band gap ≠ fixed point (L2346–2347) | N49 "Translation ≡ scaling (RG); isotropy = gauge freedom" (L10397–10554, CLAIMED in its ledger) + N21 dimensional-translation normalization (L6229–6281) | **DIRECT (2-adic core) / SPECULATION (the rest)** | AGENTS's bit-shift = 2-adic RG is PROVEN (G₃₂ = Z₂/2³²Z₂, `d>>1` = truncation map, exact on a tree — L2330–2334). The conversation's "translation ≡ scaling" is the same identity asserted for a homogeneous lattice (CL AIMED, not proven). The conversation's N25 (continuous dim = generator, integer = invariant) parallels AGENTS's frame-gauge reading but AGENTS retired "proper frame = discovery" (L2557–2559). |
| 4 | **Hexagon conjecture**, L626 ("Hexagons most densely pack circle-like shapes… Is the lattice the right shape for the semantic manifold? E=mc² analogy… Documented for later refactor investigation.") | N35 hexagonal grid (L8054–8233), N45 Eisenstein integers (L10220–11286), N55 fractal memory (L11971–12123), N69 Tau Architecture (L14342–14542) | **SPECULATION** | AGENTS's conjecture is one untested paragraph about *packing density of the semantic manifold*, explicitly "documented for later refactor investigation" (its own admission of untested). The conversation's hex lattice is a developed *hardware/address-space* architecture (hex norm, mod-6, 7ⁿ paging, ternary bijection) whose own ledger marks most claims CLAIMED/SPECULATED, only N41 enumerated. Shared content: "hexagons + lattice-of-meaning". No equation, norm, or structure transfers. See Q1. |
| 5 | **Eisenstein triangles / simplex Aₙ**, L2821–2844 (Chat-session survey #2: arc "gauge symmetry of (a−a²)/2 → e vs χ → geometric product → edge-as-primitive → spinor → Gaussian integers → Eisenstein triangles → n-dim triangle (simplex Aₙ)/unified set"; "gauge indistinguishable from the unit — signature `i² ∈ {−1,+1,0,ω}`"; open thread "whether square/triangular are one object (a real map between a²+b² and a²+ab+b², not a flag)", L2843–2844) | N45 Eisenstein ℤ[ω] (L10220–11286), N41 balanced-ternary↔7-hex bijection (L9988–10107), N48 "60° is the natural right angle" (L10220–10396) | **DIRECT (provenance — same thread) / SPECULATION (math)** | The note IS the project's record of this conversation (arc matches hexigon_graph §0: L1–2502 gauge of (a−a²)/2, L4780–5388 e vs χ, L5390–6330 GA, L8043 edge-as-primitive, L10220+ Eisenstein). So the *pointer* is direct. The *math* is not: AGENTS's own note flags square-vs-triangular as an OPEN question (not a flag — "a real map"), the simplex-Aₙ "unified set" is unproven on both sides, and the conversation's N41 is proved only by enumeration with the Lean proof proposed, not done. See Q1. |
| 6 | Signed residual r = O−E (canonical primitive, L13–15; sign analysis L1637–1653; "raw signed O−E is the best reproducibility statistic", L1655–1679) | ABSENT — "residual": 0, "O − E": 0 (hexigon_graph §4.3) | **OURS** | No counterpart on the conversation side at all. The conversation never computes observed-minus-expected. |
| 7 | Wedge = skew part `O_ab − O_ba` (curl, temporal precedence; L17–18, L990, L2678–2682; V3 "wedge=|a∧b|" RETIRED) | N16 wedge product a∧b = antisymmetric *bivector*, raises dimension (L5549–5578); N19 e↔wedge-exponential (L5579–5656, qualified "not directly" L5441) | **Name collision — NOT a mapping** | The conversation's wedge IS the GA bivector — exactly the object AGENTS RETIRED (V3: "the wedge O_ab−O_ba is the SKEW PART … NOT a bivector area… RETIRE our V3 'wedge = |a∧b|' mapping", L2678–2682). AGENTS's wedge is a *scalar difference*; the conversation's is a *grade-2 element*. Any naive cross-mapping re-imports a retired over-claim. N19 (e lives in the wedge-exponential) has no AGENTS counterpart: AGENTS's rotor is `exp(r_bwd−r_fwd)`, a *ratio*, and AGENTS explicitly says rotor ≠ wedge (L26–29, L2236–2243). |
| 8 | Rotor `exp(r_bwd−r_fwd)` — grade-0 scalar, INERT, confounded Granger (L26–29, L2236–2243); fix = even-grade ψ=(α+βI)U (L2146–2147, L2866–2867) | N18 rotor e^B = cos|B|+(B/|B|)sin|B| (L5583–5633); N32 operators as rotations (L7482–8042) | **ANALOGY (with a direction)** | The conversation's rotor is a genuine even-grade GA rotor — precisely the object AGENTS's fix aspires to ("Biggest untapped gift … the even-grade spinor structure", L2146–2147). So the conversation's N18 is AGENTS's stated upgrade path. But AGENTS's gauge survey adds a corrective: "register shift = rotor" is wrong, the gauge group is Abelian (L3073–3075) — qualifying the conversation's N32/N49 rotor-gauge flavor. |
| 9 | Spinor — `|a||b|(cos θ + I sin θ)`, the combined edge object (L27–29, L2863–2867, L2869–2929; polar form = direction(ratio) + strength(difference), L2909–2929) | "spinor" occurs once (L5439: "exponentials in GA, e^B … generate rotations in the plane (spinors)") | **ANALOGY / mostly OURS** | AGENTS's spinor is a developed statistical-geometric object; the conversation's is a passing GA reference. The polar form (ratio, difference) is AGENTS-ours. Parallel: both treat "scalar+bivector with a scale" as the whole object. |
| 10 | Polarization `f(a)/f(b) − f(b)/f(a)` = radial scale axis (L18, L1623, L1755; "causation vs gravity collapses to the polarization axis", L32, L2188–2189) | "polarization" = ferroelectric ternary-memory states (L9014–9024), polar ternary N40 (L8571–9987) | **Name collision** | Zero content overlap. AGENTS's polarization is a statistics axis; the conversation's is a memory-physics state (push/pull/null). Both OURS on their own side. |
| 11 | build_causal / causal.rs — canonical DAG builder (L44–45, L294, L1206; "causal DAG build … bytes→subwords→words→compounds, signed directional O−E", L444) | ABSENT — causal.rs/build_causal: 0 (hexigon_graph §4.3); conversation "rebuild" = FTS5 index rebuild, unrelated (L15333) | **OURS** | Pure AGENTS-side engineering. The conversation's "edge as primitive" (N34, L8043) is philosophical and unconnected. |
| 12 | Causality — Rung-1 association, "the sign/wedge is an orientation signal, never a causal arrow" (L31–32); wedge = temporal precedence NOT causal arrow (L2179–2185); "causality as suggestion" (L1973–1976) | "causal" occurs exactly ONCE: "the 60° light cone (causal structure)" inside the GR-as-edge-weights passage (L12393; N59, L12376–12551) | **Name collision — points OPPOSITE ways** | AGENTS retired causal-arrow language; the conversation uses literal light-cone causality (spacelike/timelike separation in a hex spacetime). Mapping "causal" across the two would resurrect the exact over-claim AGENTS retired. The two "causal" concepts are different objects with contrary status. See Q3. |
| 13 | Residual edge NOT a metric (L38, L2665–2669); ring = L2 norm = χ² divergence (L19, L2308–2309) | N46 hex norm = max(|a|,|b|,|a+b|), 1-cycle, ~30× faster than Euclidean (L11287–11385, CLAIMED) | **ANALOGY (cheap integer distance) with a hard conflict** | Both "integer-first, no FPU" (AGENTS: inverse-mult, no division/sqrt/float, L1137–1161; conversation: max norm, no FPU). But the objects are different norms: max norm ≠ L2. Adopting the hex norm would break `ring² = Σ(O−E)²/E` (the χ² identity that the canonical truth depends on, L19). Not a drop-in. |
| 14 | Fixed point — band gap NOT a fixed point (L35, L2346–2347); split Banach F(x)=x vs harmonic ∇·F=0 (L2673); "RG flow converges" is metaphor (L2674–2676) | "fixed point" = math fixed-point theorem + C19 "Zero is not a fixed point. It is you" (L14961) | **OURS-vs-OURS** | No math overlap. Interesting shared *reversal pattern* ("the fixed point isn't what you think"), but different objects: AGENTS's retirement is about the band gap; the conversation's is about observer-relative origin. |
| 15 | Integer register / integer fixed-point — "integer register" phrase ABSENT from AGENTS.md (map pass confirms); nearest: ISA register map A/B/X/R/F (L1209–1214), gauge.rs register IDs + transform table + GaugeSum (L3032–3060), SIMD byte-sliced fixed-point (L2996–3012), "integer (a,b) pair" gauge-int (L2840) | "integer register" ABSENT (hexigon_graph §4.3); nearest: N52 (integer, angle mod 6) representation (L11386–11553), N62 GEMM = integer add + mod-6 rotation (L11860–11970), N46 1-cycle norm | **ANALOGY** | Both sides converge on "integer-only arithmetic as the substrate", but AGENTS's is 2-adic (u32 XOR, bit-shift RG — PROVEN) while the conversation's is Eisenstein/mod-6 (ω rotation — claimed). Different groups (2^ℤ vs Z₆), different norms. |
| 16 | Rebuild — N17 directional rebuild MOOT (L439); build_causal lib rebuild (L1778) | "rebuild" = FTS5 index rebuild (L15333) | **OURS** | Unrelated usages. |
| 17 | Gauge-variant system + fold δ invariant (L2848–2861, L3035–3050, L3071–3072); "e^(iθ) is gauge-variant — maybe not optimal; the invariant is the fold δ, the angle θ is gauge-variant" (L2976–2983) | C6 "e is NOT the gauge invariant, χ is" (L5274–5305) + N13 e=engine/χ=odometer (L5156–5172) | **ANALOGY (shared reversal SHAPE, different invariants)** | Both documents perform the identical counter-to move: "the exponential/rotor quantity changes under gauge; the invariant is something else". But the invariants differ — conversation: Euler characteristic χ (topology); AGENTS: fold δ=(O−E)/E (statistics). Parallel move, not shared math. This is the single strongest *structural* correspondence between the two files. |
| 18 | Ring bands = binary-exponent strata; band gap = ridge cutoff (L572, L35, L2380–2383) | N36 7ⁿ hierarchical paging (L8147–8172, L12044–12059); N55 fractal memory, hexagonal tiling of hexagons (L11971–12123) | **ANALOGY** | Both are hierarchical scale-strata of a semantic space: AGENTS 2ⁿ frequency bands (statistical), conversation 7ⁿ address cells (physical). Same idea, different radix and different substrate. |
| 19 | Balanced ternary — ABSENT from AGENTS.md ("ternary" occurs only as "balanced Lagrangian", L666/L677 — a homonym) | N41 balanced-ternary triples q+r+s=0 ↔ 7 hex states — PROVED by enumeration, Lean proof proposed (L10056–10107); N71 polar-ternary transition energy (L12917–14341) | **OURS (conversation-side)** | The bijection is the conversation's most PROVED result; AGENTS has no ternary layer at all. Candidate extension only (see §5). |
| 20 | Quantum-on-classical — RETIRED to "data-layout coincidence, not a speedup … no BQP-type advantage follows … present but inert" (L2526–2543; UNPROVEN flag L1986–1994) | N58 superfluid spacetime / Abrikosov vortex lattice (L12292–12375, SPECULATED); N31 Ruleiad (L7445–7458, SPECULATED) | **OURS-vs-SPECULATION** | No conflict: both sides flag their own quantum-adjacent claims as unproven. AGENTS's retirement is the more disciplined of the two. |
| 21 | Gauge = the unit / signature — "third gauge position beyond the gauge survey … and gauge.rs's R⁺ scale" (L2830–2832); lands as gauge-int experiment + Signature axis (L2840–2842) | N47 "gauge and number are the same type" / Tau/6 frame (L10109–10219); N50 irrationals = gauge changes, ω is an axis (L10555–11109) | **ANALOGY→(recorded plan, still SPECULATION)** | AGENTS has ALREADY calibrated this conversation claim and filed it as a concrete experiment (integer (a,b) + compile-time Signature, ~100 lines, no .latx change — L2840–2842). The math is unbuilt; the plan is specified. |
| 22 | "physics = information" — first claimed NOT speculation (L2056–2062), later downgraded to speculation/no-paper-support (L2138) and then four-component accounting, memorization ≠ value (L2447–2450) | No direct counterpart; N31 Ruleiad closest (SPECULATED) | **OURS-vs-SPECULATION** | Both sides hold speculation-level claims; AGENTS has partially self-corrected. |
| 23 | Semantic search @ 0.023 ms — AGENTS's OWN measured number (L196, L449, L507) | N63 "Semantic search @ 0.023 ms … SQL-free" (L15281–15357) | **DIRECT (grounded cross-citation)** | The conversation cites the project's real measured capability. The lens source already embeds target-side facts — the boundary between the two documents is porous. |
| 24 | Multi-project memory distance — "the Eisenstein/Einstein frame as a project-distance metric. SPECULATION, not a result" (L1506–1518) | N49/N61 frame/gauge relativity (L10397–10554, L14863–14968) | **SPECULATION (self-labeled)** | AGENTS already tags this as a re-reading of frame dilation, deferred. The conversation's observer-relative zero (N61) is the only adjacent idea. |

---

## 2. Over-claims flagged (both directions)

### AGENTS.md's own (all admitted in-file; the lens pass confirms each is real and gives the current verdict)

1. **"Hexagon conjecture" (L626)** — untested by its own admission ("Documented for later refactor investigation"). One paragraph, E=mc² musing, zero math. Do NOT cite it as evidence for anything on the conversation side.
2. **"(1/α)·α=1 tautology" (L114, L2727)** — explicitly called numerology (philachive EmDrive "30μN forced convergence" = reverse-engineered free params + the tautology + mixed units). Canonical example of a felt-truth that is a tautology.
3. **"The lattice IS a quantum computational system — structural isomorphism" (L882–906)** — RETIRED to "data-layout coincidence, not a speedup … no BQP-type advantage … present but inert" (L2533–2543). The XOR kernel matches DJ/BV/Simon's domain; Shor/Grover do not use the WHT.
4. **"ring = Fisher information" (L800-era, L2132)** — RETIRED: inverted (Fisher ∝ 1/frequency); ring is a χ² divergence value, `ring² = Σr²` (L2305–2309).
5. **"raw flux is wrong" (L953–956)** — REVERSED 2026-08-12: raw signed O−E is the best *reproducibility* statistic (1.000/1.000/0.992); "wrong" held only for topic discovery (L1655–1669).
6. **V3 "wedge = |a∧b|" (L990)** — RETIRED: the wedge is the skew part `O_ab−O_ba`/curl, NOT a bivector area (L2678–2682). *This is exactly the conversation's wedge* — see §1 row 7.
7. **"wedge = rotor / rotor is a rotation" (field table L533)** — corrected: rotor is grade-0 scalar, inert, confounded Granger; "The rotor is NOT the wedge — split them" (L26–29, L2236–2243).
8. **"wedge ⇒ causality / temporal precedence implies causal arrow"** — RETIRED: Rung-1 association, temporal priority necessary-not-sufficient, direction unidentifiable from one joint distribution (L31–32, L2179–2185). The builder *name* `build_causal` is itself a rhetorical leftover of this.
9. **"least squares = least action" (L2082–2088)** — corrected twice: constant-action *level set* not minimizer (L2655–2659); quadratic large-deviation *rate function*, not free-energy/Euler–Lagrange (L41–42, L2442–2445).
10. **"band gap = fixed point" and "band gap = spectral gap λ₂ (Cheeger)"** — RETIRED: ridge/noise-floor cutoff; DFT Λ_cutoff (L35, L2346–2347, L2380–2383).
11. **"proper frame = single universal monofractal α≈0.5"** — REFUTED: locally monofractal with heterogeneous exponent field; "proper frame = monofractal" is a subordination/gauge, not a discovery (L36–37, L2557–2566).
12. **"fiber bundle / fibration (total space = ring × S^(n−1))"** — vacuous: discrete base ⇒ trivially trivial bundle; correct object is the fibered set; "register = gauge" mislabels external as internal (L30, L2684–2691).
13. **"register shift = rotor / gauge group"** — WRONG: gauge group R⁺/2^ℤ is ABELIAN; zero structure constants; "register shift = rotor" is wrong; non-commutativity lives in the transition matrix (L3073–3075).
14. **"the stored residual r is the gauge-invariant"** — corrected: the invariant is δ=(O−E)/E; store δ not r (L3071–3072, L3085).
15. **"rotor = α=0 Levi-Civita / zero curvature"** — RETIRED: α=0 is the curved Hellinger point; α=±1 are the flat dual geometries (L2648–2651).
16. **"natural gradient = L1"** — RETIRED: G⁻¹∇L is a contravariant vector; L1 a scalar, mirror-descent-shaped at best (L39–40, L2311–2314).
17. **"M5 p∈{1,2,4,8} flux-threshold sweep = RG flow"** — RETIRED: a persistence diagnostic, not a flow (L2336–2339).
18. **"repulsion / the anti-lattice's negative edges carry meaning"** — REFUTED empirically: repulsion scores P=0.000 on held-out data = sampling noise, not suppressed reverse events (L1671–1675, L2452–2454).
19. **"physics = information is not speculation" (L2061–2062)** — later downgraded to speculation / four-component accounting with memorization ≠ value (L2138, L2447–2450).
20. **"Shannon-1953 'lattice' = our lattice"** — name-ancestor only; a partial-order join-semilattice, mechanism does not transfer (L2614–2617).
21. **"polarization fix"** — untested flag surfaced (L439); second-order polarization register `(f(a)²−f(b)²)/T²` untested (L1776–1779).

### Conversation-side over-claims (from its own ledger + this lens; hexigon_graph §5)

1. **N19 "e lives in the wedge-exponential; χ in the integrated curvature"** — its own AI qualifies "not directly" (L5441); ANOLOGY by the conversation's own graph. And AGENTS's wedge is NOT the GA wedge (row 7), so any e↔wedge transfer is doubly blocked.
2. **N59 "GR from variable edge weights; pentagon=+60° deficit (black hole), heptagon=−60° (dark energy)"** — SPECULATED in its ledger (#19); Regge analogy, no measurement. AGENTS's own "field = residual edge graph is analogy" correction (L2693–2695) applies by parallel.
3. **N66 "ABC conjecture is a corollary of Eisenstein lattice geometry"** — SPECULATED (#23), "might actually be a corollary"; no proof on either side; AGENTS has zero ABC content.
4. **N62 "~30× AI GEMM speedup" and N71 "~1000× communication-energy reduction"** — SPECULATED (#14, #15): multiplicative estimates (10×10×3×3=900×), no measurement.
5. **N46 "hex norm 1-cycle, ~30× faster than Euclidean"** — CLAIMED (#11), hardware-cost estimate. Conflicts with AGENTS's ring = L2 (row 13).
6. **N49 "translation ≡ scaling (RG); isotropy = gauge freedom"** — CLAIMED (#16), asserted identity. The bit-shift half is supported by AGENTS's 2-adic proof; "isotropy" has zero AGENTS text.
7. **N37 "the gauge is the page table"** — CLAIMED mapping (#22); a metaphor asserted as identity; zero "page table" text in AGENTS.md.
8. **N39 "the real 10,000× comes from isotropic locality"** — CLAIMED (#26); "isotropy" absent from AGENTS.md entirely — ungrounded cross-source.
9. **N60 division-by-zero = gauge transition; N61 zero = observer position, infinity = antipode** — SPECULATED (#20, #21), projective-closure intuition.
10. **N58 superfluid spacetime ↔ Abrikosov vortex lattice** — SPECULATED (#24), analogy.
11. **N31 Ruleiad / multivector multiverse** — SPECULATED (#32), Tegmark analogy.
12. **N48 "60° is the natural right angle; i should not be 90°"** — CLAIMED (#18), philosophical/pedagogical.
13. **N41 balanced-ternary ↔ 7-hex bijection** — the one PROVED-by-enumeration result (#10), but the formal (Lean) proof is proposed, not done.
14. **N47 "gauge and number are the same type"** — CLAIMED; no AGENTS counterpart; AGENTS's own note files it as an experiment to run (L2840–2842).

---

## 3. Q1–Q4 explicit answers

### Q1 — Do the "Hexagon conjecture" (L626) + "Eisenstein triangles" (L2827–2828) map DIRECT/ANALOGY/SPECULATION to the conversation's Eisenstein 60° lattice?

**SPECULATION** — with a provenance split that matters.

- **Hexagon conjecture (L626) → Eisenstein lattice (N45): SPECULATION.** AGENTS's conjecture is one untested paragraph ("documented for later refactor investigation") asking whether hexagonal packing is the right shape for the semantic manifold, plus an E=mc² "normalization identity" musing. The conversation's Eisenstein 60° lattice is a developed architecture (hex norm, mod-6, 7ⁿ paging, ternary bijection) whose own ledger marks most of it CLAIMED/SPECULATED with only the ternary↔hex bijection enumerated. **Nothing transfers:** no norm, no group, no equation. The shared content is the word "hexagon" plus "lattice of meaning". AGENTS's conjecture is a question; the conversation is an (unverified) proposed answer — they have never been connected, and the lens pass is the first place to say so.
- **Eisenstein triangles (L2827) → Eisenstein lattice (N45): DIRECT (provenance), SPECULATION (math).** The Chat-session survey #2 note (L2821–2844) is the project's own record of THIS conversation — the arc matches hexigon_graph §0 exactly. So the pointer is direct: it IS the same thread. But the math is explicitly open: the note itself flags "whether square/triangular are one object (a real map between a²+b² and a²+ab+b², not a flag)" as an unresolved question (L2843–2844), and the simplex-Aₙ "unified set" climax (L2828) is unproven on both sides. The conversation's own ledger marks N45/N48 as CLAIMED and N41 as PROVED-only-by-enumeration.
- **Why exactly:** because AGENTS.md's only Eisenstein text is a *summary of the conversation* whose own open threads are unresolved, and the one original AGENTS.md hex item (the conjecture) is self-declared untested. Neither side supplies a derivation connecting hexagonal packing to the semantic-manifold claim. The balanced-ternary↔7-hex bijection (N41) is real math, but it lives entirely on the conversation side.

### Q2 — Is "gauge = register = ring shift" (bit-shift RG) DIRECT/ANALOGY/SPECULATION relative to "gauge is the page table" / gauge covariance O→UOU⁻¹?

**ANALOGY** (structural parallel, different objects), with two sharper sub-verdicts:

1. **"Gauge = register = ring shift" (L1417–1425) vs "gauge is the page table" (N37): ANALOGY.** Both identify gauge with a change-of-indexing/reference scheme, and both are hierarchical (2ⁿ ring bands vs 7ⁿ hex cells). But AGENTS's gauge is a *statistical normalization* — a choice of δ-formula (raw/fold/z/surprise) and of scale, with an Abelian R⁺/2^ℤ group and invariant δ (L3035–3050, L3071–3075) — while the conversation's is a *physical memory-address layout* (square vs hex MMU walk, L8224–8230). "Page table", "7^n", "DGGS", "isotropy" appear **zero times** in AGENTS.md: the conversation's page-table concept has no AGENTS-side text to map onto. The bit-shift half is the closest point: AGENTS's `d>>n` 2-adic RG is PROVEN (L2330–2334) and the conversation's "translation ≡ scaling" (N49) is the same identity asserted — so the *bit-shift = gauge* core is DIRECT-ish, but the three-name identity as a whole is ANALOGY.
2. **Gauge covariance O→UOU⁻¹ (N26) vs AGENTS's inner automorphism: DIRECT (algebra), ACTIVE-vs-INERT.** "x→RxR⁻¹ is verbatim the gauge transformation" (L2261–2264) — same conjugation. AGENTS proved its own rotor grade-0 so Ad(R)=identity (doesn't act); the conversation's operators genuinely act. So the algebra is the same; the implementation status differs.
3. **Why exactly:** because AGENTS's own gauge survey (L3062–3086) already settles the physics — gauge group Abelian, δ the invariant, ρ the genuine gauge, wedge the gauge field F=dA+A∧A — and the conversation's gauge is a different (addressing/layout) notion on top of the same word. Also "register shift = rotor" is explicitly wrong in AGENTS (L3073–3075), directly qualifying the conversation's rotor-flavored gauge talk (N32, N49).

### Q3 — Is build_causal's directional signed residual r=O−E + wedge skew DIRECT/ANALOGY/SPECULATION relative to the conversation's "hexagonal causal lattice" (60° light-cone causal structure)?

**Not DIRECT — the two sides do not share a single causal object.** Decomposed:

- **r = O−E: OURS.** The conversation has zero "residual" and zero "O−E" (hexigon_graph §4.3). AGENTS's stored primitive has no counterpart there.
- **wedge skew `O_ab−O_ba`: OURS + name collision.** The conversation's wedge is the GA bivector (N16) — the exact object AGENTS RETIRED as V3 (L990, L2678–2682). Same word, different math. Using "wedge" as a bridge would re-import a retired over-claim.
- **build_causal / causal.rs: OURS.** The conversation has neither; its "rebuild" is about FTS5 index rebuilding (L15333), unrelated.
- **The 60° light cone: conversation-side only, and contradictory in direction.** The conversation's single "causal" (L12393) is a *literal* light-cone causal structure inside the GR-from-edge-weights passage (N59). AGENTS.md has no light cone and no 60°; and AGENTS's causality doctrine is the *opposite* of a causal arrow: "the sign/wedge is an orientation signal, never a causal arrow" (L31–32), "temporal precedence, NOT the causal arrow" (L2179–2185), "causality as suggestion" (L1973–1976). **Cross-mapping "causal" would point the two sources at each other in contradiction** — AGENTS's whole causal survey exists to retire exactly the language the conversation's light cone uses.
- **Why exactly:** because the residual, the skew, and the builder are all AGENTS-only engineering, while the light cone is conversation-only speculation (N59 SPECULATED in its own ledger). The only overlap is the word "causal", and there it is a *collision*: association-orientation (AGENTS) vs spacetime cone (conversation). Verdict: **SPECULATION at best, flagged as a contradiction risk — do not bridge.**

### Q4 — Which AGENTS.md concepts would the conversation's hex-grid/balanced-ternary/Tau Architecture genuinely EXTEND or complete?

Conservative list — everything tagged SPECULATION unless derivable:

1. **gauge.rs register/gauge stack (L3032–3060) ← the conversation's "gauge = signature of the unit" (i² ∈ {−1,+1,0,ω}; N47, L10109–10219).** This is the project's OWN recorded extension (L2830–2842): a compile-time `Signature` axis in gauge.rs, integer (a,b) pair geometric product `(ac−s·bd, ad+bc)` — "square binary" (L2836–2837). **SPECULATION** (experiment not run), but fully specified (~100 lines, no .latx change).
2. **Even-grade spinor upgrade ψ=(α+βI)U (L2146–2147, L2866–2867) ← the conversation's mod-6 / ω integer rotation (N47, N52, L11386–11553).** The mod-6 group (Z₆ ⊂ SO(2)) is a discrete, exact, integer realization of the rotation the spinor fix wants — "all geometry = integer math + mod-6 rotation" (N47). **SPECULATION**: Z₆ is a subgroup of the continuous rotations; the map is plausible, not derived.
3. **The unbuilt "loop-polarity cocycle (holonomy)" / "triangle-curvature statistic" upgrade (L3086) ← the conversation's Regge deficit angles (N59: pentagon=+60° black hole, heptagon=−60° dark energy, L12466–12543).** AGENTS has the goal (discrete curvature / holonomy on the lattice) but no construction; the conversation supplies a ready-made discrete-curvature template (deficit angles on plaquettes). **SPECULATION**: both unbuilt; AGENTS's curvature language is δ/χ²-gradient-based, not lattice-defect-based.
4. **7ⁿ hex paging (N36, N55) as an alternative radix for the ring-band scale hierarchy (L572, L35).** The conversation's 7ⁿ cells are the same "scale strata of a meaning space" idea as AGENTS's 2ⁿ ring bands, at a different radix — and the multi-project memory-distance idea (L1506–1518) already invokes the "Eisenstein/Einstein frame" by name. **SPECULATION**: blocked from DERIVABLE by the radix-2 2-adic RG proof (L2330–2334) and by the fibered-set triviality constraint (L2684–2691).
5. **Hex norm = max (N46) as the ring's distance — NOT derivable, flagged as a conflict.** The hex norm is a different norm from AGENTS's ring (L2, `ring²=Σ(O−E)²/E` = χ² divergence, L19, L2308). Replacing the ring would break the χ²/L2 identities the canonical truth depends on. This is an anti-extension: the conversation's "1-cycle max" is appealing but incompatible as stated.
6. **Balanced-ternary ↔ 7-hex bijection (N41) as a 3-symbol register for the surprise register r²/E (L46).** The conversation's one PROVED result could give a push/pull/null symbol encoding. **SPECULATION**: nothing in AGENTS.md requires it, and the energy claims (N71) are model-derived, not measured.
7. **DERIVABLE (cross-documentation only):** N63 "semantic search @ 0.023 ms" is AGENTS's own measured number (L196, L449, L507) — the conversation cites it correctly; just cite both directions. And the e/χ vs e^(iθ)/δ reversal pair (§1 row 17) is a derivable *structural* parallel (both sides derive it from their own math), not an identity.

---

## 4. What the lens pass found that the map pass MISSED

1. **The two documents are the same thread.** AGENTS.md's "Chat-session survey #2" (L2821–2844) is the project's own record of the hexigon conversation (31 turns, "a solo derivation"). Neither map pass connected the two: `hexigon_graph.md` treats the conversation as an external source; `agents_graph.md` records N115 without noting it describes `hexigon_conversation.md`. Consequence: "Eisenstein triangles", "gauge = the unit", "simplex Aₙ" are not external facts to calibrate — they are the project's own landing notes with a plan (gauge-int experiment, Signature axis, spinor integer (E,w), open thread on square-vs-triangular maps).
2. **AGENTS.md's gauge survey (L3062–3086) is the authoritative calibration tool the conversation never had.** Abelian R⁺/2^ℤ gauge group ("register shift = rotor" wrong, L3073–3075), δ not r as invariant (L3071–3072), ρ as frame not gauge with labels backwards (L3067–3070), wedge = gauge field F=dA+A∧A (L3076–3077). Applied to the conversation, this downgrades N32 (operators as rotations), N49 (isotropy = gauge freedom — "isotropy" absent from AGENTS), and N37 (gauge = page table) from identity-claims to analogies, and upgrades N26 (O→UOU⁻¹) to a direct match with an active/inert caveat.
3. **The conversation embeds AGENTS-side facts.** N63 (0.023 ms semantic search, L15281–15357) cites the project's real measured lattice-lookup (AGENTS L196/L449/L507). The "lens" is not fully disjoint from the target: some conversation nodes are AGENTS facts in disguise. Any "the conversation discovered X" claim about the 0.023 ms number is wrong — it's the project's own measurement.
4. **The wedge collision is now explicit and dangerous.** Conversation wedge = GA bivector (N16) = AGENTS's RETIRED V3 mapping (L990, L2678–2682). The map pass recorded both sides' wedges but never flagged that they are the SAME WORD for DIFFERENT objects — with the AGENTS object being the one AGENTS retired. This is the single highest-risk calibration trap for any downstream survey.
5. **The "causal" collision points opposite ways.** AGENTS retired causal-arrow language (L31–32, L2179–2185); the conversation's only "causal" is a literal light cone (L12393). A naive bridge resurrects the retired over-claim. The name `build_causal` is itself flagged in AGENTS as Rung-1 association — a rhetorical leftover.
6. **The shared reversal shape.** Both files contain the same counter-to move — "the exponential/rotor quantity is gauge-variant; the invariant is elsewhere" — conversation C6 (e→χ, L5274–5305) and AGENTS L2976–2983 (e^(iθ)→δ). The map passes recorded each separately and missed that they are the same *structural* move on different invariants (χ topology vs δ statistics). This is the best genuine cross-file correspondence and it is pattern-level, not math-level.
7. **"Integer register" is absent from BOTH files** (map passes each noted the absence). The task prompt's "integer fixed-point register + bit-shift RG" resolves to: AGENTS's ISA register map (L1209–1214) + gauge.rs register IDs/GaugeSum (L3032–3060) + SIMD byte-sliced fixed-point (L2996–3012) + the 2-adic RG proof (L2330–2334) — all AGENTS-internal, DIRECT with each other, with the conversation contributing only the mod-6 alternative (N52).
8. **The hex norm conflict is a lens-level discovery.** The conversation's max-norm (N46) and AGENTS's ring=L2 (L19, L2308) are incompatible distance notions; "adopt the hex norm" would break `ring²=Σ(O−E)²/E`. Neither map pass flagged the norm conflict because neither had both sides' norms in view.
9. **The 2ⁿ-vs-7ⁿ hierarchy parallel** (AGENTS ring bands L572 vs conversation 7ⁿ paging L8147–8172/L12044–12059) is a genuine structural analogy the map passes did not align — both are "scale strata of meaning", different radix, different substrate.

---

## 5. How the hex grid could extend the rebuild (each tagged)

| Candidate extension | AGENTS hook | Conversation hook | Tag | Why |
|---|---|---|---|---|
| **Signature gauge axis + gauge-int** (integer (a,b) + compile-time `i²∈{−1,+1,0,ω}`) | gauge.rs register/gauge stack (L3032–3060); the project's own recorded "third gauge position" (L2830–2832) | N47 gauge-and-number-same-type, Tau/6 frame (L10109–10219); N50 ω as a physical axis (L10555–11109) | **SPECULATION** (specified plan, unbuilt) | AGENTS's own note lands it as a ~100-line experiment; nothing run yet. |
| **Even-grade spinor via mod-6 rotation** | ψ=(α+βI)U upgrade (L2146–2147, L2866–2867); integer-only stack (L1137–1161) | N47 mod-6, N52 (integer, angle mod 6) (L11386–11553) | **SPECULATION** | Z₆ ⊂ SO(2): plausible discrete realization of the Clifford fix, not derived. |
| **Regge deficit angles as the holonomy/cocycle upgrade** | unbuilt "loop-polarity cocycle (holonomy); triangle-curvature statistic" (L3086) | N59 pentagon=+60°/heptagon=−60° (L12466–12543) | **SPECULATION** | AGENTS has the goal without a construction; conversation supplies a discrete-curvature template. Both unbuilt; N59 itself SPECULATED. |
| **7ⁿ hex paging as ring-band addressing** | ring bands = 2ⁿ strata (L572); multi-project memory distance (L1506–1518) | N36 7ⁿ paging (L8147–8172, L12044–12059); N55 fractal memory (L11971–12123) | **SPECULATION** | Radix mismatch (2-adic proof is radix-2, L2330–2334); fibered-set triviality (L2684–2691). |
| **Balanced-ternary ↔ 7-hex bijection as a ternary register** | surprise register r²/E (L46); no ternary layer exists | N41 bijection, PROVED by enumeration (L10056–10107) | **SPECULATION** | Nothing in AGENTS requires it; energy claims (N71) model-derived, unmeasured. |
| **Hex norm = max as the ring's distance** | ring = L2 = χ² divergence (L19, L2308) | N46 max norm, 1-cycle (L11287–11385) | **CONFLICT, not an extension** | max ≠ L2; would break `ring²=Σ(O−E)²/E`. Do not adopt as a ring replacement. |
| **Cross-citation of the 0.023 ms number** | lattice-lookup measured (L196, L449, L507) | N63 (L15281–15357) | **DERIVABLE** | Same measured fact on both sides; just cite both. |

**Net:** the Tau Architecture (balanced ternary + Eisenstein lattice + fractal memory) genuinely *completes* two AGENTS-side holes — the discrete-signature gauge (gauge.rs) and the even-grade spinor (ψ=(α+βI)U) — and *supplies a template* for the unbuilt holonomy/cocycle statistic. Every one of these is SPECULATION until built/tested; none is derivable from AGENTS's current text, because AGENTS's canonical truth (signed residual, χ² ring, Abelian gauge, Rung-1 causality, fibered set) constrains the hex-grid borrowings at every point (radix, norm, causality, triviality).

---

### Standing caution (method rules 3–4, come-to-terms first)

- "wedge" (GA bivector vs skew residual), "causal" (light cone vs Rung-1 orientation), "polarization" (ferroelectric state vs radial axis), "fixed point" (math theorem vs band-gap retirement), "ternary" (balanced radix vs balanced Lagrangian), "rebuild" (FTS5 index vs lib rebuild) — six terminological collisions between the two files. Every downstream use must disambiguate before mapping.
- The one hard DIRECT bridge is the conjugation O→UOU⁻¹ = Ad(u)(x)=uxu* (active on the conversation side, inert on AGENTS's grade-0 side).
- The one hard PROVED bridge candidate is the bit-shift 2-adic RG (AGENTS, L2330–2334) ↔ conversation N49 "translation ≡ scaling" (CL AIMED) — supported for the bit-shift case, unproven for the isotropy claim.
- The lens side's own most-proved result (N41 bijection) has zero AGENTS-side anchor; AGENTS's most-proved results (signed residual reproducibility, 2-adic RG, χ² identities) have zero conversation-side anchor. The two systems genuinely share a vocabulary and an ambition, not yet a single theorem.
